// /config/tsconfig/test.json
// Typescript compiler configuration for testing the library

{
	"extends": "../../tsconfig.json",
	"compilerOptions": {
		"noEmit": true
	},
	"include": ["../../test/**/*.ts"]
}
